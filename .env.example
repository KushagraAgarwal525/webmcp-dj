# PostHog — leave both empty to disable (local, CI, forks).
# Project API key is a client token (safe in the Vite bundle). Never put a personal API key here.
VITE_POSTHOG_KEY=
VITE_POSTHOG_HOST=https://us.i.posthog.com
